LotKeys Drive Test v0.9.2.1
Build query: ?build=0921
Service worker: lotkeys-drive-test-v0921

Price intelligence update:
- Call for Price (💲❔) remains visible on the Listing but does not trigger the Listings-tab ❕.
- Vehicle-page identity is evaluated from multiple signals; one missing field no longer makes the page unavailable.
- If the normal price extractor is uncertain, LotKeys checks backward for the known Profile/Listing price on the matched page.
- ⚠️ is reserved for pages where several identity signals are missing, the page explicitly says removed/sold/not found, or the page cannot be read.
- Successful price/location evidence is saved in Website Price Finding learning observations and reused as domain-level extraction hints from synced Vehicle Profiles.
