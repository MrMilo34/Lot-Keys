LotKeys Drive Test v0.9.2.3
Build query: ?build=0923
Service worker: lotkeys-drive-test-v0923

Refresh performance update:
- Refresh Inventory now uses the normal incremental Drive/index refresh path instead of forcing a complete inventory rescan.
- Refresh Listings now uses the normal incremental Drive/index refresh path instead of re-reading every Listing JSON.
- Listing website verification is decoupled from the Refresh Listings spinner and continues in the background when due.
- A background-check guard prevents duplicate website verification runs from stacking up.
- Direct website reads time out after 7 seconds; the read-only fallback times out after 9 seconds so a slow/blocked site cannot hold the verifier indefinitely.
- Existing v0.9.2.1 alert logic remains intact, including informational 💲❔ and conservative ⚠️ behavior.
